Your goal is to update any vulnerabl dependencies

Do you following:

1. Run `pnpm audit` to find vulnerable installed packages in this project
2. Run `pnpm audit fix` to apply updates
3. Run test and verify the updates didn't break anything