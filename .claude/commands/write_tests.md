Write comprehensive test for: $ARGUMENTS

Testing conventions:

* Use Vitests wtih react Testing Library
* Place test files in a __test__ directory in the same folder as the source file
* Name test files as [filename].test.ts(x)
* Use @/ prefix for imports

Coverage:

- Test happy paths
- Test edge cases
- Test error states
- Focus on testing behavoir and public API'S rather than implementation details.
